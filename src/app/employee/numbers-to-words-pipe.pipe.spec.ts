import { NumbersToWordsPipePipe } from './numbers-to-words-pipe.pipe';

describe('NumbersToWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersToWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
